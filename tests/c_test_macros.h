#ifndef C_TEST_MACROS_H_
#define C_TEST_MACROS_H_

#define SQR(x) ((x) * (x))
#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define INDEX(i, j, m) ((m) * (j) + (i))

#endif
